# Synara

Milestone 1 includes the tensor foundation:

- `Shape`
- `Strides`
- `Storage`
- `Tensor`
- `reshape`
- `transpose`
- `indexing`
- `slicing`

Build and run:

```bash
cmake -S . -B build
cmake --build build
./build/synara
```
